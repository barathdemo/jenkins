pipeline {
    agent any
    stages {
        stage('Docker Build') {
            steps {
                echo "This is Build stage. dev triggering"
            }
        }
        stage('Docker Push') {
            steps {
                echo "This is docker push stage. dev triggering"
            }
        }
    }
}
